import { OpenIdConfiguration } from '../../openid-configuration';
import { RuleValidationResult } from '../rule';
export declare const ensureNoDuplicatedConfigsRule: (passedConfigs: OpenIdConfiguration[]) => RuleValidationResult;
