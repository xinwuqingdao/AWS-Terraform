import { InjectionToken, Provider } from '@angular/core';
import { StsConfigLoader } from './config/loader/config-loader';
import { OpenIdConfiguration } from './config/openid-configuration';
export interface PassedInitialConfig {
    config?: OpenIdConfiguration | OpenIdConfiguration[];
    loader?: Provider;
}
export declare function createStaticLoader(passedConfig: PassedInitialConfig): StsConfigLoader;
export declare const PASSED_CONFIG: InjectionToken<PassedInitialConfig>;
